generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
}

model Submission {
  id             String   @id @default(cuid())
  placeName      String
  state          String
  subZone        String
  description    String
  activity       String
  duration       String
  budget         Int
  transport      String
  accommodation  String
  emoji          String   @default("🌟")
  mapLink        String?
  imageLink      String?
  videoLink      String?
  submitterName  String
  submitterEmail String
  status         String   @default("PENDING")
  createdAt      DateTime @default(now())
}

model StateRegion {
  name     String   @id
  coords   Json     // Storing [[lat, lng], [lat, lng]]
  color    String
  subZones String[]
}
